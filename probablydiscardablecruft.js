class redditFetcher {
   constructor(subreddit) {
      this.subreddit = subreddit
   }
   
   // performFetch() {
   //    fetch('/getHotReddit', {
   //       method: 'POST',
   //       headers: {'Content-Type': 'application/json'},
   //       body: JSON.stringify({subreddit: props.chosenSubreddit})
   //    })
   // }
}